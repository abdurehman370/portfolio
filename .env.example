# Copy to .env (never commit .env). Gmail: use an App Password, not your normal password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=pc18050.abdurehman@gmail.com
SMTP_PASS=havswmrxxitadtkr
SMTP_FROM=pc18050.abdurehman@gmail.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: production build — full origin of your API (no trailing slash), e.g. https://api.yourdomain.com
# VITE_CONTACT_API_URL=
